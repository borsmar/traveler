spring:
  datasource:
    url: ${DBURL}
    username: ${PGUSER}
    password: ${PGPASSWORD}
    name: railway
  jpa:
   generate-ddl: true
   hibernate:
    ddl-auto: update
  kafka:
   bootstrap-servers: ${BROKERS}
   consumer:
    group-id: traveler-kafka-group

