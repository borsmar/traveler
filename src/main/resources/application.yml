spring:
  datasource:
    url: ${DBURL}
    username: ${PGUSER}
    password: ${PGPASSWORD}
    name: railway

  sql:
    init:
      mode: always # you won't do this in prod, I'm just doing this for demo purposes
